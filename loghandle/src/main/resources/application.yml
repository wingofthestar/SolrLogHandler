spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 1234
    url: jdbc:mysql://localhost:3306/solrlog?characterEncoding=utf8&useSSL=false
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

  data:
    solr:
      host: http://localhost:8983/solr/solrlogcore
      repositories:
        enabled: true